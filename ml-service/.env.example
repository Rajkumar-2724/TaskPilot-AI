# ===== TaskPilot AI — ML Service configuration =====

# Host/port for the FastAPI service
HOST=0.0.0.0
PORT=8000

# (Optional) Only if an external key is needed for a future model provider.
# The current service trains sklearn models locally and needs no key.